# Session Handoff

- Fecha: 2026-05-01
- Objetivo de sesion actual: reducir consumo de contexto y mejorar mantenibilidad del frontend sin romper comportamiento.
- Cambios hechos en esta sesion: extraccion de constantes puras desde `src/main.js` hacia modulos pequenos: `src/config/appConstants.js` para rutas, storage y flags runtime; `src/data/uiText.js` para textos y traducciones; `src/data/gameConstants.js` para progresiones, monedas, XP por CR, tags y navegacion superior; `src/data/itemTypeGroups.js` para jerarquia de tipos de items; `src/data/compendiumEntries.js` para normalizar Bestiario/Items/Arcanum desde CSV y resolver fuentes/imagenes/rarezas/tamanos; `src/shared/compendiumLayout.js` para la sincronizacion de alturas; `src/shared/csv.js` para `parseCsv()`; `src/shared/virtualList.js` para ventanas de listas virtualizadas; `src/shared/text.js`, `src/shared/numberUtils.js` y `src/shared/dndRules.js` para helpers puros.
- Fix aplicado tras la extraccion: `DESKTOP_ASSET_BASE_URL` vuelve a importarse en `src/main.js`, corrigiendo el error `DESKTOP_ASSET_BASE_URL is not defined` al cargar inventarios/compendios. Tambien se regenero `src/data/uiText.js` en UTF-8 valido y se comprobaron caracteres `Ñ`, tildes y ausencia de `�`.
- Cambios hechos tambien: `src/main.js` conserva el estado global, handlers, render y persistencia, pero ahora importa datos/configuracion compartida. Esto reduce el contexto que agentes futuros necesitan abrir para tareas de texto, constantes, filtros de items, normalizacion de compendios, helpers de reglas o layout de compendios.
- Tamano tras esta tanda: `src/main.js` queda en unas 14.277 lineas. Sigue siendo grande; el siguiente corte de alto impacto debe mover toolbars/filtros de compendios a `src/screens/`.
- Cambios hechos despues: se extrajo render puro de compendios a `src/screens/compendiums/detailRender.js` para fichas seleccionadas y `src/screens/compendiums/listRender.js` para filas/listas virtualizadas. Ambos reciben dependencias desde `main.js` y no leen `state` directamente.
- Documentacion actualizada: `PROJECT_CONTEXT.md`, `CURRENT_STATE.md`, `SESSION_HANDOFF.md` y `src/screens/README.md` reflejan la nueva estructura y el orden de lectura recomendado.
- Verificacion corrida: `npm run build` OK tras la extraccion.
- Riesgos activos: no hay suite automatica formal; `npm run build` valida bundle, pero conviene hacer smoke manual de Bestiario, Items, Arcanum, pantalla de personajes, tablas y guardado desktop si se empaqueta.
- Siguiente paso recomendado: extraer toolbar/filtros de Bestiario/Items/Arcanum a `src/screens/compendiums/filterRender.js`, dejando en `src/main.js` estado, seleccion, handlers y llamadas de orquestacion.

## Handoff anterior

- Fecha: 2026-04-24
- Objetivo de sesion: estabilizar la ejecutable desktop en arranque, persistencia local y sistema de guardado.
- Cambios hechos: reset fuerte de `localStorage` para la app empaquetada usando `DESKTOP_STORAGE_RESET_VERSION = 2026-04-24-c` y barrido de todas las claves `mimic-dice:*`; splash estatico inmediato en `index.html`; overlay de carga de arranque mientras cargan compendios; `campaign:save` y `campaign:save-as` ya usan y devuelven ruta completa; renderer guarda `campaignFilePath` para que `Guardar`, autosave y guardado al cerrar reutilicen archivo real; `Guardar como` ya respeta carpeta elegida; `Nueva campana` limpia storage y memoria antes de abrir `Guardar como`; `normalizeStoredCombatTrackerState()` purga la vieja semilla placeholder del combat tracker si reaparece por storage/campana antigua; cierre desktop ahora conoce si hay cambios sin guardar y evita esperar el handshake de guardado cuando no hace falta; portable mantiene `compression: store` y `portable.useZip: true`; `combatTrackerData.js` queda sin placeholders y tabla empieza vacia.
- Cambios hechos tambien: menu `Archivo` ensena ahora el fichero activo si existe; nuevo modal centrado para pedir nombre de campana antes de `Guardar campana como` y antes de `Guardar campana` cuando aun no hay fichero; el nombre confirmado alimenta el nombre de campana en memoria y el nombre sugerido del JSON en el explorador de Windows.
- Cambios hechos tambien: fix al error de cierre del main process capturando `webContents.id` antes de destruir la ventana; la app desktop abre maximizada por defecto; topbar cambia bloque `MD` por el icono real de la app; `Nueva campana` endurece el reseteo y limpia tambien la referencia al fichero activo incluso si se cancela el `Guardar como`.
- Cambios hechos tambien: icono de topbar mas grande; `preload` ahora expone firma de build del `.exe` y renderer la usa para limpiar estado viejo en el primer arranque de cada ejecutable nuevo; el empaquetado desktop excluye `dist/images/bestiary/**/*`, por lo que el portable baja a aprox. `363 MB` y deberia abrir bastante antes en la primera ejecucion.
- Cambios hechos tambien: nueva build `editable zip` mediante `electron-builder.editable.json` y script `npm run electron:build:editable-zip`; copia `dist/data` y `dist/images` a `resources/app-assets/` fuera de `app.asar`; `preload` expone `assetBaseUrl`; renderer usa esa base externa para CSV/JSON/imagenes cuando existe; workflow de GitHub Actions sube tambien artefacto `mimic-dice-windows-editable-zip`.
- Cambios hechos tambien: fix para `Failed to fetch` en la editable zip. CSV y JSON ahora se leen por filesystem desde `preload` en vez de `fetch(file://...)`; la deteccion de `resources/app-assets` prueba varias rutas relativas al `.exe` descomprimido.
- Cambios hechos tambien: fix adicional para imagenes en la editable zip. La app ya no depende de `file://` para `resources/app-assets/images`; `electron/main.js` registra protocolo custom `mimic-assets://local/...` y `preload` expone esa base al renderer para que `img src` cargue desde la carpeta descomprimida.
- Cambios hechos tambien: se monto diagnostico visible dentro de la UI para cargas fallidas de `Bestiario`, `Items` y `Arcanum`; el panel ya ensena recurso esperado, URL runtime, estrategia de carga, rutas candidatas de `app-assets` y existencia de ficheros. Ademas `loadTextAsset()` y `loadJsonAsset()` ya prueban `desktopApi.readAssetText()` aunque `hasExternalAssetDirectory` venga false.
- Cambios hechos tambien: fix de fallback para desktop empaquetado. Si la app corre sobre `file:` y `preload` no informa bien la base externa, el renderer usa igualmente `mimic-assets://local/...` en vez de caer a `data/*.csv`. Esto apunta otra vez a `resources/app-assets/data` y `resources/app-assets/images`.
- Cambios hechos tambien: `Items` se virtualizo como `Bestiario` y `Arcanum`. Ahora usa ventana visible, restaura scroll y resetea scroll al cambiar query, filtros, sort o sintonizacion, evitando render completo de toda la lista en desktop.
- Cambios hechos tambien: en desktop empaquetado se desactivo la rehidratacion de campana desde `localStorage`; al arrancar, la ejecutable ya no debe mostrar personajes, encuentros, tracker ni fichero activo heredados. `Nueva campana` se simplifico: limpia a estado en blanco, borra fichero activo y no fuerza `Guardar como` al pulsar el boton.
- Archivos tocados: `electron/main.js`, `electron/preload.js`, `index.html`, `package.json`, `src/data/combatTrackerData.js`, `src/main.js`, `src/styles.css`, `CURRENT_STATE.md`, `SESSION_HANDOFF.md`.
- Verificacion corrida: `npm run build` OK, `node --check electron/main.js` OK, `node --check electron/preload.js` OK y `npm run electron:build:portable` OK.
- Bloqueos o riesgos: portable ahora abre/cierra mejor, pero crece a aprox. `1.43 GB`; falta confirmar visualmente en uso real que `Nueva campana` ya limpia todo, que no reaparecen placeholders y que el cierre percibido mejora en el PC del usuario.
- Preguntas para usuario: ninguna abierta ahora mismo.
- Siguiente paso recomendado: probar la ejecutable nueva con un ciclo real de `Nuevo -> Guardar como -> Cerrar -> Reabrir -> Guardar -> Cargar`, revisar que el combat tracker abra vacio y medir si el cierre ya se siente rapido.
- Comando de arranque: `npm run electron:dev`
